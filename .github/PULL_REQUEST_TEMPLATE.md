<!--
Thanks for the PR!  Fill in the sections below — they make review
faster and the resulting commit history more useful.

If this is a draft / work-in-progress, open it as a draft and skip
sections that aren't ready yet.
-->

## What

<!-- One or two sentences on the user-visible change.  Skip the
"why" here; that's the next section. -->

## Why

<!-- Motivation: bug it fixes, workflow it enables, Emacs parity
gap it closes.  Link the issue number(s) if there's an open issue
(`Fixes #123`). -->

## How

<!-- Brief implementation note for non-trivial changes: what
approach you took and what you ruled out.  For one-line fixes,
"see diff" is fine. -->

## Test plan

<!-- Replace each `[ ]` with `[x]` as you verify. -->

- [ ] `make test` passes
- [ ] `make lint` clean (or stylua-formatted)
- [ ] Added or updated tests covering the change
- [ ] Manual smoke test in a real buffer (for UX-affecting changes)
- [ ] Updated `doc/organ.txt` for any user-facing config / command
- [ ] Updated `README.md` for any user-facing UX shift

## Emacs parity (if applicable)

<!-- For changes that touch syntax / agenda / TODO state / etc.,
link the Emacs reference (`org-*` defcustom, function, or Worg
section) and call out any intentional divergence. -->

## Notes for reviewers

<!-- Anything you'd flag in a synchronous review: tradeoffs you
made, alternatives you considered, parts you're least confident
about. -->
